import styled from "styled-components";

export const Button = styled.button`
    margin: 10px;
    outline: none;
    font-size: 16px;
    &::first-letter {
        text-transform: uppercase;
    }
`;